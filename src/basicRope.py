import maya.cmds as cmds

transforms = ['root','anim_GP','CTL_GP','CTL','RopeEndA','RopeEndB']
nurbsCurves = ['CTLShape','RopeEndBShape']

#Store the names of all transforms in a text file and read them to populate the scene